# 母檔更新工具

`docs/WINSTAR_Master_單一完整表_V4.1.xlsx`（或同名CSV）是正式資料母檔；網站使用的 JSON 是發布格式。

更新時請將修改後的 Excel 母檔交給 Codex，執行以下流程：

1. 檢查工作表名稱、表頭、永久 ID 與資料格式。
2. 比較上一版母檔，產生新增、修改、刪除與衝突清單。
3. 執行 Excel → JSON 轉換。
4. 驗證刀號、外徑、來源與加工參數關聯。
5. 只有驗證通過的 JSON 才替換網站資料。

請勿直接以未驗證的 Excel 或 JSON 覆蓋正式網站資料。

# GitHub Pages 發布教學

## 第一次或完整覆蓋更新

1. 解壓縮本 ZIP。
2. 進入解壓後的資料夾。
3. 按 `Ctrl + A` 選取資料夾內全部檔案與資料夾。
4. 到 GitHub 的 `winstar-tool-selector` 儲存庫，選擇 **Add file → Upload files**。
5. 把選取的全部內容拖入上傳區。必須包含 `assets`、`data`、`docs`、`update-tools` 等資料夾。
6. 等待 GitHub 列出全部檔案後，按最下方 **Commit changes**。
7. 等候約 2～5 分鐘，再重新整理網站。

## 如何確認更新成功

- 首頁右上角顯示 `V4.0.0｜197,934 筆`。
- 首頁只看到 A～E 五大官方分類。
- 在置頂刀號反查輸入 `DMC5`，應出現 C 孔加工刀具結果。
- 手機只需重新整理網頁，不需安裝更新。

## 日後只更新資料

1. 編輯 `docs/WINSTAR_Master_五大類完整母檔_V4.xlsx` 黃色欄。
2. 執行 `update-tools/更新資料.bat`。
3. 確認 `qa-results.json` 為 PASS。
4. 上傳更新後的 `data`、`docs`、`qa-results.json`。

# GitHub Pages 發布教學

## 第一次用 V4.1 覆蓋 V3.3

1. 先下載目前 GitHub 儲存庫的 ZIP 當備份。
2. 解壓縮交付的 V4.1 ZIP。
3. 打開 GitHub 上原本放 V3.3 的儲存庫，選 **Add file → Upload files**。
4. 將 V4.1 資料夾「裡面的全部檔案與資料夾」拖入上傳區；不要多包一層資料夾。
5. 確認 `index.html` 位於儲存庫最上層，輸入更新說明後按 **Commit changes**。
6. 到 **Settings → Pages**，確認來源仍為原本的分支及根目錄。
7. 等待約1至5分鐘後開啟網站，按 `Ctrl+F5` 強制重新整理。

## 上線後快速驗收

- 頁首版本顯示 V4.1.0。
- 置頂刀號反查能查整體式銑刀、孔加工及螺紋刀號。
- 五大系列按鈕完整出現。
- 外徑未填時不允許搜尋。
- 手機可直接開網址使用，不需安裝程式。

## 日後更新資料

1. 人工校正 `docs/WINSTAR_Master_單一完整表_V4.1.xlsx` 或同名 CSV；不可改動 `record_id`。
2. 新官方PDF先登記於 `docs/PDF_SOURCE_MANIFEST_V4.1.csv`，比對檔案指紋、頁碼與刀號差異。
3. 由管理端驗證新版 JSON，確認刀號唯一、外徑有效、來源與參數關聯均通過。
4. 只替換 `data` 內資料並更新 `data/version.json`、`CHANGELOG.md` 與 QA 報告，再重新上傳。

若 GitHub 顯示同名檔案即將被取代，這是正常的覆蓋更新；請勿刪除儲存庫設定或另建第二個 Pages 網站。
